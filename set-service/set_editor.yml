---
- name: Set editor to /usr/bin/vim.basic on Ubuntu
  hosts: all
  become: true
  when: ansible_distribution == 'Ubuntu'

  tasks:
    - name: Set editor using update-alternatives
      ansible.builtin.command:
        cmd: sudo update-alternatives --set editor /usr/bin/vim.basic
      register: editor_result

    - debug:
      var: editor_result


### ansible-playbook execute
# ansible-playbook -i inventory/hosts.ini set-service/set_editor.yml --limit centos7,ubuntu2204
